# WalletConnect Project ID
# Get your project ID from https://cloud.walletconnect.com/
VITE_WALLETCONNECT_PROJECT_ID=4243717e79313e7a8086903a599003a6

# API Endpoints (optional)
VITE_API_URL=http://localhost:3000

# GuardX AI Monitoring API
VITE_GUARDX_API_URL=https://asi-crash-guard-x.vercel.app

# Network Configuration
# Multi-chain support enabled
# Supported chains:
# - Testnets: Arbitrum Sepolia (421614), Base Sepolia (84532), Sepolia (11155111), Mumbai (80001), Optimism Sepolia (11155420)
# - Mainnets: Ethereum (1), Polygon (137), Arbitrum (42161), Optimism (10), Base (8453)
# Contract addresses are automatically loaded based on connected network
# See src/config/deployments.ts for chain-specific contract addresses

# Environment Mode
# Set to 'production' to enable only mainnet chains
# Set to 'development' to enable both testnet and mainnet chains
NODE_ENV=development

# Vincent Authentication Configuration
# Get your app ID from Vincent Dashboard: https://vincent.lit.dev
VITE_VINCENT_APP_ID=123456

# Vincent Delegatee Private Key
# This is the private key used for PKP wallet operations
# IMPORTANT: Keep this secret! Never commit to git or share publicly
# Generate a new one: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
VITE_DELEGATEE=your_private_key_here_without_0x_prefix

# RPC URL for blockchain connections
# Get free RPC URLs from: https://infura.io or https://alchemy.com
VITE_RPC_URL=https://arbitrum-sepolia.infura.io/v3/YOUR_INFURA_KEY